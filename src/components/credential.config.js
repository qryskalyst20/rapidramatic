//create new credential.js file in src/components folder
//use the following format:

const credentials = {
  USERNAME: "username",
  PASSWORD: "<password>",
  EMAIL: "<email>",
};

export default credentials;
